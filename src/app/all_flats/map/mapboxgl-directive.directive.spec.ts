/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MapboxglDirectiveDirective } from './mapboxgl-directive.directive';

describe('Directive: MapboxglDirective', () => {
  it('should create an instance', () => {
    const directive = new MapboxglDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
